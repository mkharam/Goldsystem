[build]
  command = "npm run build"
  publish = ".next"

# Netlify يكتشف Next.js ويشغّل الـ runtime الخاص به تلقائياً؛ نثبّت الإعداد هنا
# صراحةً حتى لا يتغيّر البناء بتغيّر الاكتشاف التلقائي.
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "22"
